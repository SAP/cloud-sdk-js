name: memory-tests

on:
  pull_request: ~
  workflow_dispatch:
  schedule:
    - cron: 0 21 * * *

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14.x'
      - run: echo "::set-output name=testKey::111"
        id: testId
      - run: echo '${{ steps.testId.outputs.testKey }}'
      - run: yarn install
        name: install test v1
        working-directory: test-packages/mermory-tests/sdk-v1/
      - run: yarn generate-vdm
        name: generate v1 test client
        working-directory: test-packages/mermory-tests/sdk-v1/
      - run: echo "::set-output name=value::$(node --inspect test-script.js)"
        name: test v1 memory usage
        id: v1Result
        working-directory: test-packages/mermory-tests/sdk-v1/
      - run: echo '${{ steps.v1Result.outputs.value }}'
      - run: npm install --no-package-lock --no-save ts-node
      - run: npx ts-node scripts/compare-memory-usage.ts 5 9
        name: compare v1 and canary

