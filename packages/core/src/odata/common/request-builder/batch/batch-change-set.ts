/* Copyright (c) 2020 SAP SE or an SAP affiliate company. All rights reserved. */

// TODO: Add specific request builder types as generics
/**
 * Representation of a batch change set, which holds a collection of write operations.
 */
export interface BatchChangeSet<T = any> {
  /**
   * Requests to combine to one change set.
   */
  readonly requests: T[];

  /**
   * Request boundary for separation of subrequests. Defaults to an autogenerated value.
   */
  readonly boundary: string;
}

// export function changeSet<T>(...requests: T[]): BatchChangeSet<T> {
//   return { requests };
// }

// export function batch<T>(
//   ...subRequests: (MethodRequestBuilderBase | BatchChangeSet<T>)[]
// ) {
//   return {
//     requests: subRequests
//   };
// }
