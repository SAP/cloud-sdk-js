import { PropertyMetadata as PropertyMetadataCommon } from '@sap-cloud-sdk/odata-common/internal';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface PropertyMetadata<ComplexT = any> extends PropertyMetadataCommon<ComplexT>{};
